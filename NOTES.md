# Timer

- Revisit whether it would make sense to allow a timer to overflow; that is, should we allow setting a timeout to be due in the past with respect to the current time (in `register_timer()`).
